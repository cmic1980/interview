package pro.caifu365.interview.loop;

public class LoopSample {
    public static void main(String[] args) {
        // for 表达式1 - 声明变量
        // for 表达式2 - 判断表达式 如果空为true
        // for 表达式3 - 步长
        for (int j = 0; j < 100; j++) {
            System.out.println("j:" + j);
        }

       // for 表达式1 - 声明变量
        // for 表达式2 - 判断表达式 如果空为true
        // for 表达式3 - 步长
        /*
        ByteBuffer dst = ByteBuffer.allocate(1024 * 1024);
        FileChannel fc = new FileInputStream("d:/video/2/misc/淫習の学園/1.wmv").getChannel();
        int i = 0;
        for (int position = fc.read(dst); position != -1; position = fc.read(dst)) {

            dst.clear();
            i = i + 1;
            System.out.println("k:" + i);
        }*/


    }
}
